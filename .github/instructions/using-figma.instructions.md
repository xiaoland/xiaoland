---
applyTo: "**/*.tsx"
description: "Apply when using Figma MCP"
---

- use `span` for text, not `p`
- avoid absolute positioning
- do not set properties that value is the same as default (eg. `text-align: left`, `word-wrap: break-word`, etc.)
